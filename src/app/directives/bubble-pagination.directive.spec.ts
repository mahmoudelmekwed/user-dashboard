import { BubblePaginationDirective } from './bubble-pagination.directive';

describe('BubblePaginationDirective', () => {
  it('should create an instance', () => {
    const directive = new BubblePaginationDirective();
    expect(directive).toBeTruthy();
  });
});
